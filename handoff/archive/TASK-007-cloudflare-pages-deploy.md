Status: CLOSED
<!-- TODO | IN_PROGRESS | DONE | CHANGES_REQUESTED | CLOSED -->

# TASK-007 -- Cloudflare Pages go-live deploy

- **Owner of this stage:** Claude (done personally -- the interactive
  Cloudflare/domain stage, not a Codex build task)
- **Repo:** `Desktop/statohub/` (where the work happens)
- **Created:** 2026-06-14 by Claude

<!-- Authoring note (Claude): keep this file plain ASCII. Use `--` not an em
dash, `->` not an arrow, `...` not an ellipsis. -->

---

## Brief  *(Claude writes -- what Codex needs to execute)*

**Goal:** Take the completed TASK-001 -> TASK-006 build live on Cloudflare
Pages as a skeleton (home + calculators only; the A5 article routes do not
exist yet). Per BUILD-PLAN A6/B6 + the Cloudflare CLI section.

**Context / inputs:**
- BUILD-PLAN.md A6 (deploy pipeline) + the "Cloudflare CLI access" section +
  the "Done means" verification list.
- Wrangler v3 pinned (Node 20.8.0 breaks v4). `wrangler@3 login` already done
  (2026-06-13, `syedjawadhassan11@gmail.com`, account
  `027007f4d056b885d434f48b4f136a07`, `pages (write)`).

**User decisions (2026-06-14):** deploy the skeleton now; Claude runs
project-create + deploy; custom domain deferred to a follow-up.

**Deliverables:**
- [x] Pre-flight gates green (build/link-gate/astro check/tests).
- [x] `pages project create statohub --production-branch main`.
- [x] First deploy of `dist/` -> live on `statohub.pages.dev`.
- [x] Post-deploy verification (trailing-slash contract, odds 404, sitemap).

**Definition of done / how to verify:**
- Live site returns 200 on `/`, `/calculators/`,
  `/calculators/standard-deviation/`; single redirect hop on the non-slash
  variant; 404 on `/calculators/odds/` + `/calculators/betting-odds/` + any
  unmatched path; sitemap lists only slash-terminated indexable URLs.

---

## Work Log  *(executed by Claude)*

- **Started:** 2026-06-14
- **Finished:** 2026-06-14

**What changed (files + where):**
- `wrangler.toml` -- added top-level `name = "statohub"`. Wrangler v3 Pages
  config validation requires it; deploy failed without it ("Missing top-level
  field name").
- `src/pages/404.astro` -- NEW. The scaffold never shipped a 404 page, so Astro
  emitted no `dist/404.html`; Cloudflare Pages then served the homepage with a
  **200 for every unmatched path** (soft-404s, and `/calculators/odds/` was
  reachable -- a direct violation of the "no odds calcs / 404 by design"
  non-negotiable). The new page is `noindex`, has no self-canonical, uses
  BaseLayout + typed `Link`, and links to home + calculators hub. Adding it
  makes Astro emit `dist/404.html`, which Pages serves with a real 404 status.
- `src/lib/content-route-ids.ts` -- regenerated by `gen-route-ids` on build
  (no semantic change).
- `handoff/TASK-006-*.md` -- reviewed + CLOSED earlier this session.

**How to verify (commands run + results):**
- `npm run build` -> exit 0; 5 pages built (now incl. `/404.html`);
  `check-links: scanned 5 pages, checked 13 internal links, found 0 violations`.
- `npx astro check` -> 0 errors / 0 warnings / 0 hints (11 files).
- `npm test` -> 33 tests / 12 files green.
- `npx wrangler@3 pages project create statohub --production-branch main` ->
  "Successfully created the 'statohub' project".
- `npx wrangler@3 pages deploy dist --project-name statohub --branch main` ->
  Deployment complete. Live at `https://statohub.pages.dev`.
- Live HTTP verification (curl):
  - `/` -> 200, `/calculators/` -> 200,
    `/calculators/standard-deviation/` -> 200.
  - `/calculators/standard-deviation` (no slash) -> **308** single hop to the
    slash version (Cloudflare's trailing-slash enforcement; functionally the
    planned single 301).
  - `/calculators/odds/` -> **404**, `/calculators/betting-odds/` -> **404**,
    `/calculators/zzz-nonexistent/` -> **404** (after the 404.astro fix; were
    200 before).
  - `sitemap-0.xml` -> only `https://statohub.com/`, `/calculators/`,
    `/calculators/standard-deviation/` (slash-terminated; 404 + noindexed proof
    route excluded).

**Blocked / couldn't do / decisions made:**
- Two interactive PowerShell `Invoke-WebRequest` attempts failed under
  NonInteractive mode; switched to `curl` for all HTTP checks.
- `seo-drift` baseline + `seo-technical` mobile/CWV pass NOT yet captured --
  deferred to a follow-up now that a live URL exists.
- Custom domain (`statohub.com` + `www`) NOT attached -- deferred per the user's
  decision. Apex canonical + single `www -> apex` 301 is the planned config.

---

## Review  *(Claude writes -- accept or send back)*

- **Reviewed:** 2026-06-14
- **Verdict:** CLOSED

**Notes / what to improve:**
- Site is live and the BUILD-PLAN "Done means" contract holds for the skeleton
  scope (trailing-slash 200s, single-hop redirect, odds 404, slash-only
  sitemap). The build pipeline TASK-001 -> TASK-007 is complete.
- The missing-404-page defect was the one real find at go-live; fixed at source
  (a permanent `src/pages/404.astro`) rather than a Pages-level redirect rule.
- **Custom domain -- DONE (2026-06-14, follow-up completed same day):**
  `statohub.com` + `www.statohub.com` attached to the Pages project via the
  Cloudflare dashboard (user step -- the supplied API token was zone-scoped, no
  account Pages:Edit). Cloudflare auto-created two proxied CNAMEs ->
  `statohub.pages.dev` + provisioned SSL. Claude added the `www -> apex` 301 via
  API: a zone Redirect Rule in the `http_request_dynamic_redirect` phase, expr
  `(http.host eq "www.statohub.com")`, target
  `concat("https://statohub.com", http.request.uri.path)`, preserve_query_string.
  Verified live: apex trailing-slash 200s; `www` -> 301 to apex (preserves path);
  odds/betting-odds/unmatched -> 404; sitemap slash-only on apex host. Live at
  https://statohub.com.
- **CI/CD -- DONE (2026-06-14, follow-up completed same day):** push-to-deploy
  wired via GitHub Actions (`.github/workflows/deploy.yml`), not native CF Git
  integration (which would force recreating this Direct Upload project +
  re-attaching the domain). Push to `main` -> astro check -> vitest -> build
  (link gate) -> `wrangler@3 pages deploy dist --project-name statohub`; deploy
  only runs if all gates pass. Auth = repo secret `CLOUDFLARE_API_TOKEN`
  (Account · Cloudflare Pages · Edit). First fully-green run verified live.
- **Follow-ups still open (own tasks, not blocking):** (1) capture the
  `seo-drift` baseline + `seo-technical` mobile/CWV pass against the live URL;
  (2) the A5 article-layout task (ArticleLayout + flat `/{slug}/` routes +
  category hubs) -- still unnumbered -- then write + ship the launch articles.